# POCO 核心模块仅依赖 Python 标准库（zipfile, xml, re）
# 以下为可选依赖
python-docx>=1.0.0

# Streamlit UI（交互式 Web 界面）
streamlit>=1.28.0

# ODF 支持（可选）
# odfpy>=1.4.1  — 高级 ODT 操作库（当前使用标准库 XML 方案，无需安装）

# PDF 导出（可选）
# LibreOffice  — 需要系统安装 LibreOffice 用于 DOCX/ODT → PDF 转换
